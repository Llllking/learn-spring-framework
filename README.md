I created a GameRunner class that runs games it takes game as a parameter and it runs it using the methos run()
In my project i created a game object which is an instance of the MarioGame class and i run it using GameRunner class
this is tight coupling
observe that if i wanted to run a new class i need to make a class of that game make instance make a contructor of GameRunner class that accepts that game
